version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "arn:aws:ecs:${AWS_REGION}:${AWS_ACCOUNT_ID}:task-definition/rag-system"
        LoadBalancerInfo:
          ContainerName: "rag-api"
          ContainerPort: 3000